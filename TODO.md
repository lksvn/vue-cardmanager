## TODO List:
- When normalizing the types, use the type mapping too
- Add an order by A->Z/Z->A in the collection viewer
- Add support to order param of the search (https://scryfall.com/docs/api/cards/search)
- Add support to dir param of the search (with the options: auto / asc / desc)
- Add support to unique param of the search (with the options: cards / prints / art)
- Better layout of the prints list in CardsDetails
- Quick add action (adds to the collection without any grouping tag) in the grid and list view
- Better separation of functions, files and utils
- Better UI design